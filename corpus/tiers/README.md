# Corpus tiers

Each tier is an independent Corpus root. A tier becomes buildable only after its reviewed `scope.json` and `coverage_matrix.csv` are finalized. Generated payloads remain local according to `corpus/.gitignore`.

`scope_draft.json` is advisory and must not be used as extraction authority.
